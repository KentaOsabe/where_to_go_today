# タスク

- [x] 1. 一覧取得APIの基盤を追加する
- [x] 1.1 ルーティングに index を追加する
  - `GET /api/places` を追加する
  - _Requirements: 1.1_

- [x] 1.2 一覧取得のコントローラ処理を実装する
  - `created_at` 降順で取得する
  - `page` / `per` は 1 以上で受け付ける（未指定時は 1 / 50）
  - `per` は最大 50 にクランプする
  - _Requirements: 1.1_

- [x] 1.3 レスポンスにページング情報を含める
  - `places` と `pagination` を返す
  - 空配列でも `pagination` を返す
  - _Requirements: 1.1_

- [x] 2. フロントエンドのルーティングとAPI連携を整備する
- [x] 2.1 React Router を導入し画面遷移を構成する
  - `/` は `/places` にリダイレクト
  - `/places` を一覧画面に割り当てる
  - `/places/:id` を詳細画面に割り当てる
  - `/register` を登録フォームに割り当てる
  - _Requirements: 1.1, 3.1, 3.2_

- [x] 2.2 一覧取得APIクライアントを追加する
  - `fetchPlaces({ page, per })` を実装する
  - _Requirements: 1.1_

- [ ] 3. 一覧画面を実装する
- [x] 3.1 一覧画面の状態管理を実装する
  - ローディング/空状態/エラー/再試行を扱う
  - _Requirements: 1.3, 1.4, 1.5_

- [x] 3.2 一覧アイテムの表示を実装する
  - 店名リンクで `/places/:id` に遷移できるようにする
  - 来店ステータス、URL、任意情報を表示する
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 3.3 登録導線を追加する
  - 一覧画面上部に `/register` への登録ボタンを配置する
  - _Requirements: 1.4, 3.2_

- [x] 3.4 ページングUIを実装する
  - 前へ/次へボタンを提供する
  - `?page=` クエリに同期する
  - _Requirements: 1.1_

- [x] 3.5 登録完了後の遷移を更新する
  - 登録成功時に `/places` へ遷移する
  - _Requirements: 3.1, 3.2_

- [ ] 4. テストで一覧機能を担保する
- [ ] 4.1 バックエンドの一覧取得を検証する（RSpec）
  - 複数件取得と `created_at` 降順を確認する
  - `page` / `per` のページングを確認する
  - 空配列の返却を確認する
  - _Requirements: 1.1_

- [ ] 4.2 フロントエンドの一覧表示を検証する（frontend/tests）
  - 取得成功時の表示を確認する
  - ローディング/空状態/エラー表示と再試行を確認する
  - 登録ボタンから `/register` へ遷移できることを確認する
  - _Requirements: 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 3.2_
