# コード規約・構成
- フロントとバックを明確に分離（`/frontend`, `/backend`）。
- React コンポーネントは `PascalCase.tsx`、コンポーネント名も PascalCase。
- TypeScript の関数名は camelCase。
- Ruby/Rails のファイル・関数は snake_case。
- フロントは UI と状態/ロジックの分離、バックは MVC レイヤに沿って責務分割。
- パスエイリアスは未導入（必要に応じて）。
