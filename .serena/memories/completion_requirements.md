# 作業完了時の期待
- 変更範囲に応じて関連テストを実行し、実行できない場合は理由を明記する。
- フロントは `npm run lint` / `npm run test` を優先的に検討。
- バックは `bin/rails test`、必要に応じて RuboCop（任意）も検討。
- 仕様フェーズは Requirements → Design → Tasks → Implementation の順で人間レビューを挟む。